#!/usr/bin/env bash

source ~/scripts/hub.sh
git config --global user.name "clnelson"
git config --global user.email "dumb.droid.dev@outlook.com"
git config --global credential.helper "cache --timeout=7200"

#echo "" >> ~/.bash_functions && echo "" >> ~/.bash_functions
#cat ~/scripts/functions >> ~/.bash_functions
#sudo apt install fortune cowsay toilet -y
